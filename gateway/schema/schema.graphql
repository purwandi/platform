type Schema {
  query: Query
  mutation: Mutation
}

type Query {
  viewer(): User!
}

type Mutation {
  updateUserProjectRole(input: [Int!]!): User!
}

type User {
  id: String!
  username: String!
  email: String!
  fullname: String
  callname: String
}
